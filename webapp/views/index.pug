extends layout

block content
  div.uk-background-muted
    h1= title
    p Welcome to #{title}
    if user
      dev
        a(href="/logout") #{user.username} をログアウト

    else
      dev
        a(href="/login") ログイン
      